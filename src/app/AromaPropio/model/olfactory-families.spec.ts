import { OlfactoryFamilies } from './olfactory-families';

describe('OlfactoryFamilies', () => {
  it('should create an instance', () => {
    expect(new OlfactoryFamilies()).toBeTruthy();
  });
});
